# Chat Trace Protocol

> A structured protocol for webview chat messages, attachments, entity verification metadata, and tool execution traces.. Intent: This concept should exist because the webview file defines a cohesive UI-facing contract that blends conversation state with verification and tool telemetry, which is richer than a generic webview model.

**Table:** `N/A`  
**Storage:** N/A  

