# Instance Authentication Actor Context

> Data model representing the authenticated actor's context including UUID and legacy status.. Intent: To encapsulate authentication results for use by downstream components requiring actor identity and mode.

**Table:** `N/A`  
**Storage:** N/A  

