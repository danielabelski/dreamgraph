# Multi-Instance Config Snapshot

> A composite snapshot of all configuration, registry, and semantic definitions active in a given instance at bootstrap or runtime.. Intent: Needed to model the persistent, forensic state for instance isolation and cross-instance operations such as federation and migration.

**Table:** `N/A`  
**Storage:** N/A  

