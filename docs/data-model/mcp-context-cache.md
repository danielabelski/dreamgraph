# MCP Context Cache

> A structured in-memory cache for holding context slices and deep-insights data collected via MCP tools, used to build LLM input contexts.. Intent: Should exist to represent the architectural pattern that persists and manages context slices across fetchers, prompts, and LLM invocations.

**Table:** `N/A`  
**Storage:** N/A  

