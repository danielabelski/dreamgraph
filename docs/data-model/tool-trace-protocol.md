# Tool Trace Protocol

> A structured trace payload for reporting tool execution details such as arguments, affected files, duration, and outcome to interactive clients.. Intent: This concept should exist because the webview protocol already defines a reusable execution-trace schema that connects tool activity to user-visible diagnostic context.

**Table:** `N/A`  
**Storage:** N/A  

