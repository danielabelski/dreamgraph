# Tool Trace Contract

> A structured protocol payload for reporting tool execution summaries, file impact, timing, and status into the VS Code webview.. Intent: The webview protocol already defines ToolTraceEntry as a reusable abstraction; promoting it would expose a cross-cutting observability contract between tooling and UI.

**Table:** `N/A`  
**Storage:** N/A  

