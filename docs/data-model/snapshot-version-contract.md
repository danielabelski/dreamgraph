# Snapshot Version Contract

> A compatibility contract between daemon-provided graph snapshots and the Explorer client build enforced at fetch time.. Intent: This concept belongs in the graph because the API code treats snapshot version matching as a hard runtime boundary, making compatibility enforcement a distinct architectural concern rather than just an implementation detail.

**Table:** `N/A`  
**Storage:** N/A  

