# Bootstrap Fingerprint Guard

> A per-instance guard that records seen LLM configuration fingerprints and decides whether bootstrap actions should fire again.. Intent: This concept captures the exact-once bootstrap protection implied by the registry and makes the configuration identity rule explicit in the graph.

**Table:** `N/A`  
**Storage:** N/A  

