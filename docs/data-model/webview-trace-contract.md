# Webview Trace Contract

> A UI-facing protocol for representing tool executions, attachment snapshots, and entity verification state in conversational views.. Intent: This concept should exist because the webview protocol bundles chat messages, attachments, tool traces, and verification metadata into a unified interaction contract not captured by current entities.

**Table:** `N/A`  
**Storage:** N/A  

