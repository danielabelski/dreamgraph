# Discipline Manifest Rules

> A schema capturing the machine-readable discipline execution rules, including tool classification, phase permissions, and state transition logic.. Intent: Should exist to encapsulate the core logic on which tool registration, permissions, and session lifecycle depend.

**Table:** `N/A`  
**Storage:** N/A  

