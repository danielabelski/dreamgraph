# Webview Trace Payload

> A UI-facing payload shape combining tool execution trace, entity verification status, and chat attachments for conversational inspection surfaces.. Intent: This concept should exist because the webview protocol defines several adjacent structures that together imply a richer diagnostic message contract than plain chat messages alone.

**Table:** `N/A`  
**Storage:** N/A  

