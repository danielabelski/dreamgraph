# UI Migration Plan

> A structured plan that compares source and target UI platforms, including element mappings, gaps, and evidence for migration decisions.. Intent: The UI registry emits migration planning types and should be represented as a reusable artifact, not only a tool result.

**Table:** `N/A`  
**Storage:** N/A  

