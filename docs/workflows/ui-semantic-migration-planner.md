# UI Semantic Migration Planner

> A planning abstraction that compares semantic UI elements across platforms and generates adaptive migration steps and gap analysis.. Intent: This should exist because the UI registry exposes migration evidence, validation context, gap elements, ported elements, and plan steps as a coherent workflow beyond simple storage.

**Trigger:** derived from grounded behavioral evidence  

