# Instance Authentication Validation Workflow

> A workflow that validates the instance identity on incoming requests by checking headers against active scopes.. Intent: To enforce instance-level authentication consistently across all endpoints that require security validation.

**Trigger:** derived from grounded behavioral evidence  

