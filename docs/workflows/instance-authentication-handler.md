# Instance Authentication Handler

> A workflow component that validates instance authentication headers and manages legacy mode fallback.. Intent: To secure instance-scoped API calls and provide standardized error responses for authentication failures.

**Trigger:** derived from grounded behavioral evidence  

