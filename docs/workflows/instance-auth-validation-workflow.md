# Instance Authentication Validation Workflow

> A workflow that validates incoming requests against the active instance UUID, ensuring secure scope isolation.. Intent: To provide a standardized process for enforcing instance authentication and prevent unauthorized access.

**Trigger:** derived from grounded behavioral evidence  

