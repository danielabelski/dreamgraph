# Authentication Context Resolution

> An abstraction that resolves the active authentication context including actor UUID and legacy mode flags based on instance lifecycle and request headers.. Intent: To centralize the logic for constructing authentication context objects from request metadata and instance lifecycle state, enabling consistent authentication workflows.

**Repository:** dreamgraph  
**Domain:** security  
**Status:** discovered  

**Tags:** dream-promoted, derived_hub

