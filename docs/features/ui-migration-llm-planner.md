# UI Migration LLM Planner

> A planning layer inside the UI registry that uses LLM routing and message structures to generate or refine migration outputs.. Intent: This should exist because UI registry tooling imports both route selection and LLM message types, implying a hidden planning component that bridges semantic UI elements with cognitive generation.

**Repository:** dreamgraph  
**Domain:** ui  
**Status:** discovered  

**Tags:** dream-promoted, derived_hub

