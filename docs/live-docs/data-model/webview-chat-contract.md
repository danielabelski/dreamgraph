# Webview Chat Contract

> The typed message and attachment protocol exchanged with the VS Code webview chat experience, including verification and tool trace metadata.. Intent: This concept should exist because protocol.ts defines a reusable interaction schema that ties together chat UI, attachments, verification states, and execution traces across the extension.

**Table:** `N/A`  
**Storage:** N/A  

