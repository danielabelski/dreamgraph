# Entity Link Cross-Metadata

> A data model abstraction capturing the rich metadata and link references connecting entities across the graph.. Intent: Should exist because both data_model and webview/protocol.ts encode cross-link types and references, yet there's no graph-wide abstraction capturing these enriched edge semantics.

**Table:** `N/A`  
**Storage:** N/A  

