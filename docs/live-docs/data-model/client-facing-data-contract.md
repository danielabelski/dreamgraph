# Client-facing Data Contract

> Versioned objects and message bindings structuring API façades, persistence bridges, and 'webview' flows visible/accessible to extension or external client code.. Intent: Should exist to track rigorous requirements for structural stability, forward schema-compat regeneration, message/payload verification in flows between daemon, webview, explorer client, and potentially SDK-bound callers, tying directly to exported interface types in code.

**Table:** `N/A`  
**Storage:** N/A  

