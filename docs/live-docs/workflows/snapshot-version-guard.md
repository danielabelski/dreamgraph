# Snapshot Version Guard

> A compatibility gate that prevents the explorer from consuming graph snapshots with unsupported schema versions.. Intent: This should exist as a first-class workflow because the client explicitly checks version compatibility and throws a dedicated error to protect the SPA from newer daemon snapshots.

**Trigger:** derived from grounded behavioral evidence  

