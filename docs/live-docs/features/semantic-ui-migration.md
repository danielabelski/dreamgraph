# Semantic UI Migration Planning

> A planning capability that compares source and target platforms using semantic UI elements and generates migration gaps, mappings, and future steps.. Intent: The UI registry exposes a distinct migration-planning abstraction that spans registry data, LLM routing, and plugin-provided UI definitions.

**Repository:** dreamgraph  
**Domain:** ui  
**Status:** discovered  

**Tags:** dream-promoted, derived_hub

