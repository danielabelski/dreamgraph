# Instance Authentication Legacy Mode Manager

> Manages legacy mode authentication where no UUID scope is active, allowing backward compatibility while still supporting audit recording.. Intent: To enable a controlled legacy mode that bypasses strict isolation without compromising auditability of authentication actors.

**Repository:** dreamgraph  
**Domain:** security  
**Status:** discovered  

**Tags:** dream-promoted, derived_hub

