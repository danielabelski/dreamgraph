# Instance Authentication Gatekeeper

> A centralized component that validates instance authentication headers to enforce security boundaries for API requests.. Intent: To provide a consistent security boundary by verifying instance identity and legacy mode fallback for all instance-scoped API calls.

**Repository:** dreamgraph  
**Domain:** security  
**Status:** discovered  

**Tags:** dream-promoted, derived_hub

