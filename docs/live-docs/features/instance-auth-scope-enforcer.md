# Instance Authentication Scope Enforcer

> A feature that enforces validation of the instance scope on incoming requests by verifying instance UUID headers and legacy mode flags.. Intent: To centralize and standardize instance scope validation logic and ensure requests are correctly scoped to active instances or legacy mode.

**Repository:** dreamgraph  
**Domain:** security  
**Status:** discovered  

**Tags:** dream-promoted, derived_hub

