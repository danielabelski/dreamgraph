# Instance Authentication Scope Validator

> Validates the incoming request instance UUID against the active scope and enforces legacy mode fallback if no scope is active.. Intent: Provides a reusable authentication helper to ensure instance isolation and secure request validation.

**Repository:** dreamgraph  
**Domain:** security  
**Status:** discovered  

**Tags:** dream-promoted, derived_hub

