# Action Checklist

Reference plugin for the declarative standalone Architect tab SDK. Install this folder through the normal instance plugin discovery path. The plugin registers `examples.action-checklist.checklist`, requires an explicit plan id, persists state through `ctx.architect.planState`, and exposes only host-rendered checklist JSON snapshots.
